import React, { JSX } from 'react';
import { View } from 'react-native';

import { Screen } from '@components';

export function SignIn(): JSX.Element {
  return (
    <Screen>
      <View>SIGN IN PAGE</View>
    </Screen>
  );
}
